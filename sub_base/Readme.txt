业务基础模块
    简介： 此模块作为所有业务组件的基础模块使用，专注于各个组件共用的业务处理，比如网络请求基础字段解析。另外业务模型层的接口也
放在这一模块中，业务模型接口的实现安放在相应的组件中,从而使这一模块看起来像是个“业务插座”。一旦某个业务组件参与编译，那么相当于
对应业务被插到了"插座"上,对应的业务也就可以工作了。由于业务模型接口放在这一模块，也就要求对应的数据模型（bean）也要放在这里。
同时因为业务模型通知也是被全局使用的，所以通知接口（观察者接口）也放到了这里。主要部分如下：
    1.网络请求结果解析基础
    2.业务模型接口+业务数据模型+业务模型消息通知